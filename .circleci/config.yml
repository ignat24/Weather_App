version: 2.1

workflows:
  test-deploy:
    jobs:
      - test

jobs:
  test:
    docker:
      - image: cimg/base:2020.01
    steps:
      - checkout
      - run: docker --version
     