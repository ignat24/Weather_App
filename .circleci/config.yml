version: 2.1

workflows:
  test-deploy:
    jobs:
      - test

jobs:
  test:
    docker:
      - image: cimg/base:2020.01
    steps:
      - checkout
      - setup_remote_docker
      - run: 
          name: Docker version
          command: docker --version
      - run:
          name: Docker login
          command: docker login -u $DOCKER_USER -p $DOCKER_PASS
     