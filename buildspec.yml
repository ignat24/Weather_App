version: 0.2

env:
  variables:
    CI: "true"
    REPOSITORY_REGION: "eu-central-1"
    ENV: "dev"
    APP: "weather"
    NAME_CONTAINER: "main"

phases:
  build:
    commands:
      - ls -la
      - docker --version

  post_build:
    commands:
      - aws ecr get-login-password --region eu-central-1 | docker login --username AWS --password-stdin 873827770697.dkr.ecr.eu-central-1.amazonaws.com
      - cd app
      - docker build -t weather-dev .
      - docker tag weather-dev:latest 873827770697.dkr.ecr.eu-central-1.amazonaws.com/weather-dev:0.2
      - docker push 873827770697.dkr.ecr.eu-central-1.amazonaws.com/weather-dev:0.2